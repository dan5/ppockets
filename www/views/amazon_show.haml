
.custam_notice== @player.nameが編集した商品を情報を表示しています。

%p todo: params
%p todo: 説明を表示する。
%p todo: ドロップアイテム

.amazon_show.c
  %h2= link_to h(unescape_html @item.title), @item.detailpageurl
  .image
    - img_url = @item.largeimage_url
    - img = "<img src='#{img_url}'>"
    = link_to img, "/amazon/#{h @item.asin}"
  .l
    %ul.spec
      %li== asin: #{h @item.asin}
      %li== 価格￥#{h @item.amount}
      %li== auther: #{h @item.author}
      %li== publisher: #{h @item.publisher}
  %hr

%p
  todo: amazon.co.jpの関連商品
